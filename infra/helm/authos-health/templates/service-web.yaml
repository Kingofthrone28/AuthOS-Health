apiVersion: v1
kind: Service
metadata:
  name: {{ include "authos.fullname" . }}-web
  labels:
    {{- include "authos.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "authos.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
